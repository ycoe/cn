<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd

        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd"
       default-lazy-init="false">

    <description>Spring公共配置</description>

    <context:component-scan base-package="com.duoec.cn">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="annotation"
                                expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
    </context:component-scan>

    <context:property-placeholder
            ignore-resource-not-found="true"
            ignore-unresolvable="true"
            properties-ref="localProperties"
    />

    <bean id="localProperties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
        <property name="fileEncoding" value="UTF-8"/>
        <property name="localOverride" value="true"/>
        <property name="locations">
            <list>
                <value>file:${configRootPath}/server.properties</value>
                <value>file:${configRootPath}/common.properties</value>
            </list>
        </property>
    </bean>

    <import resource="classpath:spring/spring-cache.xml" />
    <import resource="classpath:spring/spring-mongo.xml" />
    <!-- 配置文件加载路径检查, 默认使用JETTY_HOME变量, 要与property-placeholder配置域中的location配置保持一致. -->
    <!--<bean class="com.duoec.common.config.SpringConfigurePathCheckBean">-->
    <!--<property name="configRootPathKey" value="configRootPath" />-->
    <!--</bean>-->
</beans>
